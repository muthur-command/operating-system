include("images-rockchip.cfg")

image "${IMAGE_NAME}.img" {
	size = "${DISK_SIZE}"

	hdimage {
		partition-table-type = "gpt"
		align = 512
	}

	include("partition-spl-spl.cfg")
	include("partitions-rockchip-ab.cfg")
}

# rockchip-recovery

Vendored Rockchip `external/recovery` (updateEngine only) for Neardi LB200 OTA.

Builds `/usr/bin/updateEngine` when `BR2_PACKAGE_ROCKCHIP_RECOVERY_UPDATEENGINE` is enabled.

Source lives in `src/` (from Rockchip Linux SDK). License: see `NOTICE`.

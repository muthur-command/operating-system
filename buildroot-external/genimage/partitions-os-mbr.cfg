partition mcos-boot {
	size = ${BOOT_SIZE}
	partition-type = 0xc
	bootable = "yes"
	image = "boot.vfat"
}

partition mcos-kernel0 {
	partition-type = 0x83
	size = ${KERNEL_SIZE}
	image = "kernel.img"
}

partition mcos-system0 {
	partition-type = 0x83
	size = ${SYSTEM_SIZE}
	image = ${SYSTEM_IMAGE}
}

partition mcos-kernel1 {
	partition-type = 0x83
	size = ${KERNEL_SIZE}
}

partition mcos-system1 {
	partition-type = 0x83
	size = ${SYSTEM_SIZE}
}

partition mcos-bootstate {
	partition-type = 0x83
	size = ${BOOTSTATE_SIZE}
}

partition mcos-overlay {
	partition-type = 0x83
	size = ${OVERLAY_SIZE}
	image = "overlay.ext4"
	forced-primary = "yes"
}

partition mcos-data {
	partition-type = 0x83
	size = ${DATA_SIZE}
	image = ${DATA_IMAGE}
	forced-primary = "yes"
}
